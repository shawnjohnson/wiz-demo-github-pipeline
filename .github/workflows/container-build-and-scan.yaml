name: Build and Scan Container Image

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-scan:
    name: "Wiz-cli Demo Build and Scan Container Image"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # Required for GitHub Package Repository
      security-events: write # Required for SARIF upload
      
      # only required for workflows in private repositories
      actions: read
    env:
      SCAN_PATH: "." # Set the relative path in the repo to build the image from
      TAG: "latest" # Set the tag to use for the image
      POLICY: "shawnjohnson-build-audit-only" # Set the desired Wiz CLI policy to use
      REGISTRY: ghcr.io
      IMAGE_NAME: ${{ github.repository }}

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Log in to the Container registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Container Image
      id: build-image
      uses: docker/build-push-action@v6
      with:
        context: . # Assuming Dockerfile/Containerfile is in the root
        file: Containerfile # Or Containerfile, adjust as needed
        # Important: Only push after security scans pass
        push: false
        # Important: Load the built image into the local Docker daemon
        load: true
        tags: "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}"
        # Output the image digest, which can be useful later for verification if needed
        outputs: type=image,annotation=org.opencontainers.image.base.name=${{ env.IMAGE_NAME }}

    - name: Download Wiz CLI
      run: curl -o wizcli https://downloads.wiz.io/wizcli/latest/wizcli-linux-amd64 && chmod +x wizcli

    - name: Authenticate to Wiz
      run: ./wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
      env:
        WIZ_CLIENT_ID: ${{ secrets.WIZ_CLIENT_ID }}
        WIZ_CLIENT_SECRET: ${{ secrets.WIZ_CLIENT_SECRET }}

    - name: Docker Info
      # Debugging docker tooling
      run: docker info

    - name: Run wiz-cli docker image scan
      id: wiz-scan
      # run: ./wizcli docker scan --image $TAG --policy "$POLICY"
      run: |
        sudo ./wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
        # sudo is required to ensure access to host file systems where the layer data is present
        sudo ./wizcli docker scan --image "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.TAG }}" \
         --output wiz-vuln-scan.sarif,sarif \
         --policy "$POLICY" \
         --driver mountWithLayers
        
      env:
        WIZ_CLIENT_ID: ${{ secrets.WIZ_CLIENT_ID }}
        WIZ_CLIENT_SECRET: ${{ secrets.WIZ_CLIENT_SECRET }}

    - name: Upload SARIF results to GitHub Security
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: wiz-vuln-scan.sarif

    - name: Push Container Image (only after successful scan)
      # This step uses a direct 'docker push' command.
      # It pushes the image that was already built and loaded locally in the first step.
      # This entirely avoids triggering a second build.
      run: docker push "${{ env.FULL_IMAGE_NAME_TAG }}"

    - name: Fetch digest of Docker image for Graph enrichment
      run: |
        echo "Listing local docker images after push:"
        docker image list
        echo "Listing local docker images with digests after push:"
        docker images --digests
        # Now the image has been pushed, and its digest is stable.
        ./wizcli docker tag --image "${{ env.FULL_IMAGE_NAME_TAG }}"

